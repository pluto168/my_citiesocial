  <div class="tabs is-centered">
      <ul>
        <% categories.each do |category| %>
          <li><%= link_to category.name, category %></li>   <%# category = category_path(category) %>
        <% end %> 
      </ul>
    </div>


